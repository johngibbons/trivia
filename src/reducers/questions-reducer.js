import { Map } from 'immutable'
import { Question } from '../models/Question';

const questions = (state = Map(), action) => {
  switch (action.type) {
  default:
    return state;
  }
}

export default questions;
